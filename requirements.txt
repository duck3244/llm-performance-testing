# 오픈소스 LLM 추론 성능 최적화 시스템 필수 패키지

# 핵심 라이브러리
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# 머신러닝 및 최적화
scikit-learn>=1.0.0
scikit-optimize>=0.9.0  # 베이지안 최적화용
torch>=2.0.0
transformers>=4.35.0

# 오픈소스 LLM 추론 엔진
# vllm>=0.2.0  # 고성능 추론 (CUDA 환경에서만)
# text-generation>=0.6.0  # Text Generation Inference 클라이언트
# ollama>=0.1.0  # Ollama 클라이언트

# 모델 최적화
optimum>=1.14.0  # HuggingFace 최적화 도구
bitsandbytes>=0.41.0  # 양자화
accelerate>=0.24.0  # 모델 로딩 최적화

# 한국어 처리
sentence-transformers>=2.2.0  # 의미적 유사도 계산용
# konlpy>=0.6.0  # 한국어 형태소 분석 (선택사항)

# 데이터 처리
datasets>=2.14.0  # HuggingFace 데이터셋
jsonlines>=3.1.0
pyyaml>=6.0

# 시각화
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.17.0
kaleido>=0.2.1  # plotly 이미지 저장용

# 시스템 모니터링
psutil>=5.9.0
pynvml>=11.4.1  # GPU 모니터링
GPUtil>=1.4.0  # GPU 유틸리티

# 비동기 처리
asyncio
aiofiles>=22.1.0

# 웹 요청
requests>=2.28.0
httpx>=0.24.0  # 비동기 HTTP 클라이언트

# 설정 및 로깅
python-dotenv>=1.0.0
click>=8.0.0  # CLI 개선용 (선택사항)

# 수치 계산 및 통계
scikit-image>=0.19.0  # 이미지 처리 (선택사항)
statsmodels>=0.13.0  # 통계 분석

# 진행률 표시 및 유틸리티
tqdm>=4.64.0
colorama>=0.4.6  # 컬러 출력
tabulate>=0.9.0  # 테이블 형식 출력
rich>=13.0.0  # 풍부한 터미널 출력

# 개발 및 테스트 (선택사항)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# Jupyter 노트북 지원 (선택사항)
jupyter>=1.0.0
ipywidgets>=8.0.0
jupyterlab>=3.5.0

# 추가 최적화 라이브러리 (선택사항)
# intel-extension-for-pytorch  # Intel CPU 최적화
# deepspeed>=0.12.0  # 분산 학습 및 추론
# onnx>=1.14.0  # 모델 최적화
# onnxruntime>=1.16.0  # ONNX 런타임

# 메모리 프로파일링 (개발용)
memory-profiler>=0.60.0
pympler>=0.9

# 설치 가이드:
# 1. 기본 설치: pip install -r requirements.txt
# 2. GPU 가속 (CUDA): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 3. vLLM 설치 (고성능 추론): pip install vllm
# 4. Ollama 설치: pip install ollama
# 5. 전체 개발 환경: pip install -r requirements.txt && pip install pytest black flake8